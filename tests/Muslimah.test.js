import { Muslimah } from '@al-mabsut/muslimah';
// Purpose of this test is to validate that the rollup to cjs/esm is successful.

describe('Muslimah', () => {
  it('should work as expected', () => {
    expect(Muslimah).toBeDefined();
  });
});
