import { Hidayah } from '@components/Hidayah';
import content from '@hanafi/en/13_ruling_istihaadah.md';

export const Istihadah = ({ style }) => (
  <div>
    <Hidayah content={content} style={style} />
  </div>
);
