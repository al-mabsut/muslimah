import { Hidayah } from '@components/Hidayah';
import * as content from '@hanafi/en/8_ruling_bleeding_most_likely_istihaadah.md';

export const MostLikelyIstihadah = ({ style }) => (
  <div>
    <Hidayah content={content} style={style} />
  </div>
);
